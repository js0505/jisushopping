import React, { useEffect } from 'react';
import axios from 'axios';

const ProfileScreen = () => {

    useEffect(() => {
        
    }, [])

    return (
        <div>
            <h1>Profile</h1>
            
        </div>
    );
};

export default ProfileScreen;